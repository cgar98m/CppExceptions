#pragma once

#include <windows.h>

namespace Utils
{
    namespace Parser
    {
        //////////////////////////
        // Tipos de argumentos  //
        //////////////////////////
        
        enum ArgumentType
        {
            SOLO_ARGUMENT = 0,
            INTEGER_ARGUMENT,
            STRING_ARGUMENT
        };
    };
};
